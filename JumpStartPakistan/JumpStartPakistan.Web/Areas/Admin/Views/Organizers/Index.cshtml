@model IEnumerable<JumpStartPakistan.Domain.Entities.Organizer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Organizers List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@if (!string.IsNullOrEmpty(ViewBag.msg))
{
    <div class="alert alert-success fade in">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
        <p>@ViewBag.msg</p>
    </div>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrganizerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrganizerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrganizerId })
        </td>
    </tr>
}

</table>
